# Тест-кейсы: Ручное тестирование REST API (JSONPlaceholder)

## Объект тестирования: Эндпоинт `/posts`

### Кейс 1: Успешное получение существующего поста (GET)
- **Предусловие:** Пост с ID=1 существует в системе.
- **Шаги:**
  1. Отправить GET-запрос на `https://jsonplaceholder.typicode.com/posts/1`.
- **Ожидаемый результат:**
  - Код статуса: `200 OK`.
  - Тело ответа содержит корректный JSON с полями: `userId`, `id`, `title`, `body`.
  - Значение поля `id` равно 1.
- **Фактический результат:** *[Заполняется после выполнения]*
- **Статус:** Pass / Fail

### Кейс 2: Создание нового поста (POST)
- **Предусловие:** Нет.
- **Шаги:**
  1. Отправить POST-запрос на `https://jsonplaceholder.typicode.com/posts`.
  2. В теле запроса (JSON) передать:
     ```json
     {
       "title": "QA Test Post",
       "body": "This is a test created during API learning.",
       "userId": 1
     }
     ```
- **Ожидаемый результат:**
  - Код статуса: `201 Created`.
  - В теле ответа возвращается созданный объект со всеми отправленными полями и новым полем `id` (например, 101).
- **Фактический результат:** *[Заполняется после выполнения]*
- **Статус:** Pass / Fail

### Кейс 3 (Негативный): Попытка получить несуществующий пост (GET)
- **Шаги:**
  1. Отправить GET-запрос на `https://jsonplaceholder.typicode.com/posts/99999`.
- **Ожидаемый результат:**
  - Код статуса: `404 Not Found` (или `200` с пустым объектом, в зависимости от реализации API).
- **Фактический результат:** *[Заполняется после выполнения]*
- **Статус:** Pass / Fail

### Кейс 4: Полное обновление поста (PUT)
- **Предусловие:** Существует пост с ID=1.
- **Шаги:**
  1. Отправить PUT-запрос на `https://jsonplaceholder.typicode.com/posts/1`.
  2. В теле запроса (JSON) передать ВСЕ обязательные поля:
     ```json
     {
       "id": 1,
       "title": "Updated Title via PUT",
       "body": "This post has been fully updated.",
       "userId": 1
     }
     ```
- **Ожидаемый результат:**
  - Код статуса: `200 OK`.
  - В теле ответа возвращается обновлённый объект. Поле `title` равно `"Updated Title via PUT"`.
- **Фактический результат:** *[Заполнить после теста]*
- **Статус:** Pass / Fail

### Кейс 5: Частичное обновление поста (PATCH)
- **Предусловие:** Существует пост с ID=1.
- **Шаги:**
  1. Отправить PATCH-запрос на `https://jsonplaceholder.typicode.com/posts/1`.
  2. В теле запроса (JSON) передать ТОЛЬКО поле для обновления:
     ```json
     {
       "title": "Updated Title via PATCH only"
     }
     ```
- **Ожидаемый результат:**
  - Код статуса: `200 OK`.
  - В теле ответа возвращается объект. Поле `title` изменилось, остальные поля (`body`, `userId`) остались прежними.
- **Фактический результат:** *[Заполнить после теста]*
- **Статус:** Pass / Fail

### Кейс 6: Удаление поста (DELETE)
- **Предусловие:** Существует пост с ID=1.
- **Шаги:**
  1. Отправить DELETE-запрос на `https://jsonplaceholder.typicode.com/posts/1`.
- **Ожидаемый результат:**
  - Код статуса: `200 OK` (или `204 No Content`).
  - Тело ответа пустое или содержит подтверждение.
- **Фактический результат:** *[Заполнить после теста]*
- **Статус:** Pass / Fail
---
*Примечание: Это учебные кейсы для практики. Сервер JSONPlaceholder не сохраняет данные, поэтому POST всегда возвращает успех с симуляцией.*
